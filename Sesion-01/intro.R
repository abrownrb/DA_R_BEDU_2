ejemplo_suma <- 2+3 # tomo dos valores, los sumo y los guardo en la variable llamada ejemplo_suma
ejemplo_multiplicacion <- 3*5

ejemplo_multiplicacion - ejemplo_suma


nombre <- "Alicia"

install.packages('dplyr') 

install.packages("dplyr") 
library(dplyr)

base::mean(c(2,3))
sum(2,3, NA, na.rm = FALSE) # no ignoro los NA
sum(2,3, NA, na.rm = TRUE) # ignorando los NA
